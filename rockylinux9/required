




*Roles/Permissions -> chức năng:
user normal 	   -> upload file, CRUD file minh` upload (user->id === posts->user_id)
area manager       -> user normal, + delete và edit visibility file trong cùng area(North,Central,South)
super admin	  	   -> all permissions
=>file public không cần login vẫn download được

*Roles/Permissions:
	UserNomal:
		-view file, edit file, delete file, create file
	Area(N,C,S):
		-view file, edit file, delete file, create file 
		-view file area, edit file area, delete file area, public file area

	SuperAdmin:
		-all permissions

*DB 

-area : id, name 

-users: id, fullname, email, password, area_id

-files: id, file_name, file upload, description, user_id, area_id, visibility('private','area','public'), created_at, updated_at 

-permissions (view, create, update, delete)

-roles (normal user, manager area, super admin, guest)

=> (users 1 - n files n  - 1 areas 1 - n users)

*init permissions, roles, model super admin

*Display

-Guest: hiển thị all files public và download
-Auth : 
	+ Login
	+ UserNormal:   1 List files của mình -> CRUD & download
	+ Area Manager: UserNormal và List Của Area(N,C,S) -> edit(visibility), delete, download 
	+ Super Admin :  

Muốn view files area South -> user hasRoles('AreaManager') && user hasArea('South')  
Khi user create file, mặc định là private, khi AreaManager set area để các user cùng area xem và set public để ai cũng được xem, dropdown visibility('private','area','public')

share: comit	abc123


User permission: view profile, edit profile, CRUD file của mình, view + download file public
Area_manager: view user in area, CRUD file chính mình, CRUD file user in area
=> user.view, file.create, file.view, file.update, file.visibility, file.delete

User Normal: File: Do -> file.create, edit.file, delete.file, view+download file public
                   Not -> CRUD file of user khác
             User: Do -> user.profile, user.edit profile
                   Not -> Tự gắn quyền và thao tác trên acc của user khác

Area Manager: File: Do -> file.create của mình, edit.file all, delete.file all, file.view, file.visibility, view + download file public
					Not -> CRUD file of area khác
			  User: Do -> user.view
			        Not ->  			



X - chuyển response->json sang api
- đổi btn close->create and create another
X - xóa file->roles & permissions và các file thừa  
- kiểm tra thiết bị user dùng bằng lib: jenssegers/agent, điều chỉnh UI
- Thêm tính năng view file: click vào sẽ mở file 
